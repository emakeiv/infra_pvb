[tool.poetry]
name = "infra-pvb"
version = "0.1.0"
description = ""
authors = ["emakeiv"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
sqlalchemy = "^2.0.23"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
